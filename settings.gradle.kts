rootProject.name = "kotlin-mc-hub"

pluginManagement {
    repositories {
        gradlePluginPortal()
        maven("https://maven.fabricmc.net/")
        maven("https://maven.quiltmc.org/repository/release/")
        maven("https://server.bbkr.space/artifactory/libs-release/")
    }
}
